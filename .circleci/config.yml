version: 2.1

workflows:
  version: 2.1
  build:
    jobs:
      - convert
      - build:
          requires:
            - convert
          filters:
            branches:
              only: main
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: main

jobs:
  convert:
    working_directory: ~/quickstart
    docker:
      - image: circleci/python:3.7.6
    steps:
      - checkout
      - run: 
          name: Install packages
          command: |
            git submodule sync
            git submodule update --init --recursive 
            pip install jupyter
            pip install nb2hugo
            touch logs.txt
            python convert-to-md.py -d
      - persist_to_workspace:
          root: .
          paths: content
  build:
    docker:
      - image: cibuilds/hugo:latest
    steps:
      - checkout
      - run:
          name: "build"
          command: hugo
      - persist_to_workspace:
          root: .
          paths: public
  deploy:
    machine:
      enable: true
    steps:
      - checkout
      - run:
           name: Deploy Over SSH
           command: |
             ssh hugo@192.168.107.138 "python -m http.server 8000"
